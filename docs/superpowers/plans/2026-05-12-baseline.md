# 重构基线 (2026-05-12)

## vitest (npm run test:all)
- 通过: 213
- 失败: 0
- 耗时: 163.85s (17 test files)

## self-test (npm run test:legacy)
- 通过: 615
- 失败: 5 (列出如下)
- 耗时: ~312s

### 失败测试列表
| 测试名 | 说明 |
|--------|------|
| `mcp-inject: mock-agent received mcpServers` | MCP 注入测试失败 |
| `log-time: timestamp format valid` | 日志时间戳格式校验失败 |
| `node.log: snapshot is empty (logs are not replayed) — expected 0, got 3` | node.log 快照非空 |
| `integ-cleanup: buffer seeded before stop — got 0 updates` | 集成清理测试缓冲区为空 |
| `model-info: server logged usage_update or context size change` | model-info 日志未找到预期输出 |

## tsc --noEmit
- errors: 0
- 错误列表：无

## 说明
此为重构前基线。后续每个 Task 完成后均需保持此基线（数字可以增加但不能下降）。
若 baseline 中已有失败测试，重构过程中不强求修复，但需在完成报告中标注。

上述 5 个 legacy 失败测试为重构前已有问题，不计入重构引入的回归。
